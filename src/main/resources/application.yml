server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test_local_2
    username: juwonlee
    password: glass
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        auto_quote_keyword: false
        globally_quoted_identifiers: false
        format_sql: true
        show_sql: true

  logging:
    level:
      org:
        hibernate:
          type:
            descriptor:
              sql: trace

payment:
  toss:
    test_client_api_key : test_ck_ADpexMgkW36eDqeea798GbR5ozO0
    test_secrete_api_key : test_ck_ADpexMgkW36eDqeea798GbR5ozO0
    success_url :
    fail_url :

# thymeleaf 설정
thymeleaf:
  prefix: classpath:templates/
  suffix: .html