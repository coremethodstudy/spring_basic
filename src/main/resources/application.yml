server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test_local_2
    username: juwonlee
    password: glass
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        auto_quote_keyword: false
        globally_quoted_identifiers: false
        format_sql: true
        show_sql: true

  logging:
    level:
      org:
        hibernate:
          type:
            descriptor:
              sql: trace

#payment:
#  toss:
#    test_client_api_key :
#    test_secrete_api_key :
#    success_url :
#    fail_url :